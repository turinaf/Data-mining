## Movie classification prediction
This is django web app project
Django must be installed to run it.
Then create virtual environment. Since I installed django in Anaconda virtual environment, it's recommended that you use anaconda too.
Then run the following command

```
$ cd webapp
$ python manage.py runserver
```
